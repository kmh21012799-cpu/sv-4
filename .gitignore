__pycache__/
*.pyc
.ipynb_checkpoints/
consistency/**/__pycache__/
data/*.npz
data/*.log
results/*.log
results/*.npz

# ★ Grid data is committed on purpose (reproducibility — see SOURCE.txt).
#   Never ignore it. A lost grid is what forced the C3b reimplementation.
!results/grid/
!results/grid/*.npz

# ★ Paper Poincare arrays are committed on purpose (reproducibility).
#   Never ignore them — losing arrays to .gitignore forced a recompute earlier.
!paper/data/
!paper/data/*.npz

# MATLAB figure delivery bundle (duplicated data) + zip are artifacts
paper/matlab_figs/
paper/*.zip
